# Base URL of the deployed backend API. In local development, leave unset so
# requests use relative /api paths (the Vite dev server proxies them to
# http://localhost:8000). Set to your deployed backend for production builds.
VITE_API_BASE_URL=http://localhost:8000
